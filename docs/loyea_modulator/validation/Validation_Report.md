# 验证记录

版本：1.0.0-prototype。所有输入均为人工构造的解码结果或宿主事件。

- 34 项测试通过，耗时 1.011 秒；完整日志见 `test_log.txt`。
- 32 组 OCEAN 边界人格，各 500 步随机事件，共 16,000 步，检查数值范围和缓存上限。
- 100 组随机初始状态验证时间分段一致；独立 RK4 积分验证闭式解。
- 7 组跨语言回放序列，共 50 次观测，保存输入、状态和期望输出。

| 测量范围 | p50 | p95 | p99 |
|---|---:|---:|---:|
| 调制器，8事件／8活动痕迹 | 0.1469 ms | 0.2089 ms | 0.3659 ms |
| 加128条Lore候选筛选 | 0.1811 ms | 0.2549 ms | 0.4283 ms |

环境：Python 3.12.14，Linux-6.18.35-x86_64-with-glibc2.39；预热300次，测量6,000次。当前满缓存序列化检查点 2878 字节，**不等于实际堆内存**。

已验证：有限状态有界、闲置恢复、主客体区分规则、证据关联、重试、快照校验、离散强度滞回、输出词表和Lore预算。

未验证：旧感知模型在真实中文输入上的准确率，角色自然度，Android真机耗时、发热和耗电，仓库中的持久化事务。计时不含分词、ONNX、数据库、网络和LLM。

`dynamics.png` 展示重复敌意事件、一次关联修复、随后无输入的解析轨迹；它是工程仿真，不是用户对话实验。
